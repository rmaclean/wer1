mutation findOne($name: String!, $artistName: String!) {
  findOne(name: $name, artist_name: $artistName) {
    IRSC
    artist_name
    created_at
    duration
    id
    name
    release_date
    updated_at
  }
}
