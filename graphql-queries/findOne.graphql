query FindOne($name: String!, $artistName: String!) {
  findOne(name: $name, artist_name: $artistName) {
    id
    IRSC
    artist_name
    created_at
    duration
    name
    release_date
    updated_at
  }
}
